# Leave ROBOT_SERIAL_PORT empty (or omit it) for Simulation Mode.
# Set it only when you intentionally attach physical hardware.
# ROBOT_SERIAL_PORT=/dev/ttyUSB0
ROBOT_BAUDRATE=115200

# Flask server (localhost by default)
ROARM_API_HOST=127.0.0.1
ROARM_API_PORT=5005
